{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost/fada-dos-cilios/assets/html/index.html",  // Caminho correto
            "webRoot": "${workspaceFolder}"
        }
    ]
}
